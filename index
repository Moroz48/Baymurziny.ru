<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Приглашение на свадьбу | Рустам и Анастасия</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Great+Vibes&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --dark-olive: #5b6b45;
            --light-olive: #8a9c73;
            --gold: #c5a880;
            --gold-hover: #b0926a;
            --beige: #fdfbf7;
            --dusty-rose: #c0979f;
            --text-dark: #2c3e2e;
        }

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Montserrat', sans-serif;
            color: var(--text-dark);
            /* Насыщенный оливковый фон для всего сайта */
            background: linear-gradient(135deg, var(--dark-olive) 0%, var(--light-olive) 100%);
            scroll-behavior: smooth;
        }

        .hero {
            position: relative;
            height: 100vh;
            /* Вы можете вставить свою картинку вместо этой ссылки */
            background: url('https://images.unsplash.com/photo-1519225421980-715cb0215aed?q=80&w=2000&auto=format&fit=crop') center/cover no-repeat;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            padding: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0, 0, 0, 0.5); /* Более темный фон для яркого текста */
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            padding: 40px;
            border: 2px solid var(--gold);
            border-radius: 15px;
            background: rgba(44, 62, 46, 0.4);
            backdrop-filter: blur(5px);
        }

        .invite-title {
            font-family: 'Great Vibes', cursive;
            font-size: 4rem;
            color: var(--gold);
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 4.5rem;
            margin: 0;
            font-weight: 600;
            letter-spacing: 2px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .date {
            font-size: 1.8rem;
            margin-top: 20px;
            letter-spacing: 4px;
            font-weight: 500;
            color: var(--beige);
        }

        /* Оформление карточки */
        .section {
            padding: 60px 40px;
            max-width: 800px;
            margin: -60px auto 60px auto; /* Залезает на картинку сверху */
            text-align: center;
            background: var(--beige);
            border-radius: 20px;
            position: relative;
            z-index: 3;
            box-shadow: 0 20px 50px rgba(0,0,0,0.2);
            /* Внутренняя золотая рамочка */
            outline: 2px solid var(--gold);
            outline-offset: -15px;
        }

        h2 {
            font-family: 'Great Vibes', cursive;
            font-size: 3.5rem;
            color: var(--dark-olive);
            margin-bottom: 20px;
            font-weight: 400;
        }

        p {
            line-height: 1.8;
            font-size: 1.15rem;
            margin-bottom: 20px;
        }

        .divider {
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 40px 0;
        }

        .divider::before, .divider::after {
            content: '';
            height: 1px;
            background: var(--gold);
            width: 100px;
            margin: 0 15px;
        }

        .divider-icon {
            color: var(--gold);
            font-size: 1.5rem;
        }

        .dress-code-colors {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 25px 0;
        }

        .color-circle {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 2px solid var(--gold);
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        .highlight-box {
            background-color: white;
            padding: 30px;
            border-radius: 15px;
            border: 2px dashed var(--gold);
            margin: 40px 0;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .highlight-box h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            color: var(--dark-olive);
            margin-top: 0;
        }

        /* Форма */
        .rsvp-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
            text-align: left;
            margin-top: 30px;
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.05);
        }

        input[type="text"] {
            width: 100%;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-family: 'Montserrat', sans-serif;
            box-sizing: border-box;
            background: #fafafa;
            transition: border 0.3s;
        }

        input[type="text"]:focus {
            border-color: var(--gold);
            outline: none;
        }

        .checkbox-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 15px;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 10px;
            cursor: pointer;
        }

        button {
            background-color: var(--gold);
            color: white;
            border: none;
            padding: 18px 30px;
            font-size: 1.2rem;
            font-weight: 500;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Montserrat', sans-serif;
            margin-top: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 4px 15px rgba(197, 168, 128, 0.4);
        }

        button:hover {
            background-color: var(--gold-hover);
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .invite-title { font-size: 3rem; }
            h1 { font-size: 3rem; }
            .section { padding: 40px 20px; margin-top: -30px; outline-offset: -10px; }
            .checkbox-group { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <header class="hero">
        <div class="hero-content">
            <div class="invite-title">Приглашение на свадьбу</div>
            <h1>Рустам & Анастасия</h1>
            <div class="date">19 ИЮНЯ 2026</div>
        </div>
    </header>

    <div class="section">
        <h2>Дорогие друзья и родные!</h2>
        <p>Мы рады сообщить, что мы женимся! Приглашаем вас разделить с нами этот особенный день, наполненный любовью, радостью и отличным настроением.</p>
        <p>Для вас будет работать великолепная <b>фотозона</b>, где мы сможем сделать самые красивые и памятные совместные кадры на стильном фоне.</p>

        <div class="divider"><span class="divider-icon">🌿</span></div>

        <h2>Дресс-код</h2>
        <p>Мы будем благодарны, если вы поддержите концепцию нашей свадьбы:<br><b style="font-size: 1.3rem; color: var(--dark-olive);">«Оливковая элегантность»</b></p>
        <p>Приветствуются пастельные тона:</p>
        <div class="dress-code-colors">
            <div class="color-circle" style="background-color: var(--light-olive);" title="Оливковый"></div>
            <div class="color-circle" style="background-color: #e6daca;" title="Бежевый"></div>
            <div class="color-circle" style="background-color: var(--dusty-rose);" title="Пыльная роза"></div>
        </div>
        <p><i>Огромная просьба: пожалуйста, избегайте белого (этот цвет только для невесты) и полностью чёрного цветов.</i></p>

        <div class="highlight-box">
            <h3>Вместо цветов 🥂</h3>
            <p>Не ломайте голову над букетами! Мы будем очень рады, если вместо традиционных цветов вы подарите нам бутылочку вашего любимого алкоголя на ваш вкус для нашего будущего домашнего бара.</p>
        </div>

        <div class="divider"><span class="divider-icon">💍</span></div>

        <h2>Анкета гостя</h2>
        <p>Пожалуйста, подтвердите свое присутствие и ответьте на пару вопросов. Анкета автоматически сформирует сообщение и переведет вас в Telegram для отправки ответа.</p>

        <form class="rsvp-form" id="rsvpForm" onsubmit="sendToTelegram(event)">
            <div>
                <label for="guestName"><b>Ваше Имя и Фамилия:</b></label>
                <input type="text" id="guestName" placeholder="Например: Иван Иванов" required>
            </div>

            <div>
                <label for="plusOne"><b>Будете ли вы с парой?</b> (Укажите имя или оставьте пустым):</label>
                <input type="text" id="plusOne" placeholder="Имя спутника/спутницы">
            </div>

            <div>
                <label><b>Что вы предпочитаете из напитков?</b> (можно выбрать несколько):</label>
                <div class="checkbox-group">
                    <label class="checkbox-item"><input type="checkbox" name="alcohol" value="Водка"> Водка</label>
                    <label class="checkbox-item"><input type="checkbox" name="alcohol" value="Вино"> Вино</label>
                    <label class="checkbox-item"><input type="checkbox" name="alcohol" value="Самогон"> Самогон</label>
                    <label class="checkbox-item"><input type="checkbox" name="alcohol" value="Шампанское"> Шампанское</label>
                    <label class="checkbox-item"><input type="checkbox" name="alcohol" value="Безалкогольные напитки"> Безалкогольные</label>
                </div>
            </div>

            <button type="submit">Отправить ответ в Telegram</button>
        </form>
    </div>

    <script>
        function sendToTelegram(event) {
            event.preventDefault(); 

            // ВАЖНО: ЗАМЕНИТЕ "Ваш_Telegram_Username" на ваш реальный ник в телеграме (без @)
            const tgUsername = "slepovA1"; 

            // Собираем данные
            const name = document.getElementById('guestName').value;
            const plusOne = document.getElementById('plusOne').value;
            const alcoholInputs = document.querySelectorAll('input[name="alcohol"]:checked');
            
            let alcoholList = [];
            alcoholInputs.forEach((checkbox) => {
                alcoholList.push(checkbox.value);
            });

            // Формируем текст сообщения
            let message = `🔔 *Ответ на приглашение (19 июня)*\n\n`;
            message += `👤 Гость: ${name}\n`;
            
            if (plusOne.trim() !== "") {
                message += `👥 Будет с парой: ${plusOne}\n`;
            } else {
                message += `👥 Будет один/одна\n`;
            }

            if (alcoholList.length > 0) {
                message += `🍷 Напитки: ${alcoholList.join(', ')}\n`;
            } else {
                message += `🍷 Напитки: Не выбрано\n`;
            }

            // Создаем ссылку для перехода в Telegram
            const tgUrl = `https://t.me/${tgUsername}?text=${encodeURIComponent(message)}`;
            
            // Открываем Telegram в новой вкладке
            window.open(tgUrl, '_blank');
        }
    </script>

</body>
</html>
